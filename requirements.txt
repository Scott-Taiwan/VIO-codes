requests>=2.28
tqdm>=4.64
numpy>=1.21

# OpenCV — choose ONE of the following lines:
# On Jetson (JetPack): OpenCV is already installed system-wide; skip pip install.
# On a regular Linux/Mac for development:
opencv-python>=4.5
